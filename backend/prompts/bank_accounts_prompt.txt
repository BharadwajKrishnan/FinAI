<Task>
Your task is to extract all the bank account information from the complete document provided by the user.
Extract all the bank account information from the entire document.
</Task>

<Important>
1. Extract only bank account information. Do not extract details related to stocks, mutual funds, fixed deposits or commodities.
2. All the bank account details that you have extracted must be exactly as per the values in the document. Do not modify any values.
3. Return the bank accounts in a JSON format. Do not include any other text in your response. Only return the JSON.
4. If there are no bank account information in the document, then return an empty JSON array [].
5. Make sure that there are no duplicate account numbers in the JSON list. All the account numbers in the JSON list must be unique.
</Important>

<Output Format>
Each JSON object MUST have the following keys with EXACT names:
1. "Bank Name" - Extract the name of the bank EXACTLY as shown in the document
2. "Account Number" - Extract the account number EXACTLY as shown in the document.
3. "Current Balance" - Extract the current account balance EXACTLY as shown in the document.
4. "Owner Name" - Primary account holder's name. If not provided, use "self"
</Output Format>

<Family Member Details>
The following family members have been added by the user. Use this information to identify which family member each bank account belongs to by matching the account holder name in the document with the names below.
{family_members}

When extracting the "Owner Name" field:
1. Match the account holder name from the document with the family member names listed above (case-insensitive matching)
2. If you find a match, use the exact name as shown in the family members list above
3. If no match is found or the account holder name is not clearly visible in the document, use "self" as the Owner Name
4. The Owner Name should be one of the names from the family members list, or "self" if it's the primary user's account
</Family Member Details>

<Document>
Here is the complete document uploaded by the user. The document may contain multiple pages separated by "--- Page Separator ---".
{page}
</Document>

