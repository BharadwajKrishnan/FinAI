<Task>
Your task is to extract all the stock/equity information from the complete document provided by the user.
Extract all the stock/equity information from the entire document.
</Task>

<Important>
1. Extract only stocks/equities (shares of companies). Do not extract details related to mutual funds, bonds, bank accounts, fixed deposits or commodities.
2. All the stock details that you have extracted must be exactly as per the values in the document. Do not modify any values.
3. Return the stocks in a JSON format. Do not include any other text in your response. Only return the JSON.
4. If there are no stock/equity information in the document, then return an empty JSON array [].
5. Make sure that there are no duplicate stocks in the JSON list. All the stocks in the JSON list must be unique.
6. DO NOT PERFORM ANY CALCULATIONS WHATSOEVER. ONLY EXTRACT VALUES EXACTLY AS THEY APPEAR IN THE DOCUMENT.
7. Make sure that the JSON format is correct. Please check the JSON format before returning the JSON array.
</Important>

<Output Format>
Each JSON object MUST have the following keys with EXACT names:
1. "Stock/Equity Name" - Extract the name EXACTLY as shown in the document
2. "Stock Symbol" - Extract the ticker symbol EXACTLY as shown (e.g., "AAPL", "RELIANCE", "TCS"). If not available, use the stock name as symbol
3. "Average Price" - Extract the average purchase price EXACTLY as shown in the document. Look for "Avg. Price", "Average Price", or "Purchase Price" labels. Extract the EXACT value - do not round, modify, or calculate.
4. "Current Price" - Extract the current market price EXACTLY as shown in the document. Look for "Price", "Current Price", or "Market Price" labels. Extract the EXACT value - do not round, modify, or calculate.
5. "Quantity" - Extract the quantity EXACTLY as shown in the document. Extract the EXACT value - do not round, modify, or calculate.
6. "Purchase Date" - Extract purchase date in YYYY-MM-DD format. If not available in document, use "1900-01-01" as placeholder
7. "Value at Cost" - Extract the total amount invested EXACTLY as shown in the document. Look for "Value at Cost", "Amount Invested", or "Total Invested" labels. Extract the EXACT value as it appears - DO NOT calculate it. DO NOT multiply Average Price * Quantity. The document already shows this value - copy it EXACTLY.
8. "Current Value" - Extract current market value EXACTLY as shown in the document. Look for "Current Value", "Current Worth", or "Market Value" labels. Extract the EXACT value - do not round, modify, or calculate.
9. "Owner Name" - Primary holder's name. If not provided, use "self"
</Output Format>

<Family Member Details>
The following family members have been added by the user. Use this information to identify which family member each stock belongs to by matching the account holder name in the document with the names below.
{family_members}

When extracting the "Owner Name" field:
1. Match the account holder name from the document with the family member names listed above (case-insensitive matching)
2. If you find a match, use the exact name as shown in the family members list above
3. If no match is found or the account holder name is not clearly visible in the document, use "self" as the Owner Name
4. The Owner Name should be one of the names from the family members list, or "self" if it's the primary user's account
</Family Member Details>

<Document>
Here is the complete document uploaded by the user. The document may contain multiple pages separated by "--- Page Separator ---".
{page}
</Document>
